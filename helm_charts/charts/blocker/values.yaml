appName: blocker

port: 8080

configmap:
  name: blocker
  data:
    RESOURCE_SERVER_JWT_ISSUER_URI: "http://keycloak.{{ .Release.Namespace }}.svc.cluster.local:8080/realms/master"

image:
  name: blocker
  tag: latest